{
  "version": 2,
  "routes": [
    { "src": "/api/(.*)", "dest": "/app.js" },
    { "src": "/socket.io/(.*)", "dest": "/app.js" }
  
  ],
  "functions": {
    "app.js": {
      "maxDuration": 30
    }
  }
}